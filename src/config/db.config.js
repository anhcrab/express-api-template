import { postges } from '../setting.js';

export default {
  postgreSQL: {
    user: postges.user,
    host: postges.host,
    database: postges.database,
    password: postges.password,
    port: postges.port,
  },
  mySQL: {
    // Database details
  },
  mongoDB: {
    // Database details
  },
};
